# Rule: checkpoint drafts

id: checkpoint_drafts
agent_id: writer
enforcement: strict
description: >
  Stop at section boundaries for approval

on_violation: stop_and_report

checks:
  - id: checkpoint_drafts_check
    description: Stop at section boundaries for approval
    action: Agent must halt, report violation, and remediate before continuing.

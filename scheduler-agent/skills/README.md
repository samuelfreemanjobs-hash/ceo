# Scheduler Agent — skills

| Skill | File | Purpose |
|-------|------|---------|
| `meeting-scheduling` | [`meeting_scheduling_skill.md`](../meeting_scheduling_skill.md) | Core scheduling rules, heuristics, safety |

Loaded into system prompt at run time via `SCHEDULER_SKILL_PATH`.

Entry: `scheduler-agent`

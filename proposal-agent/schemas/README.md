# Proposal Agent — schemas

| Schema | Use |
|--------|-----|
| [`brief.v1.json`](brief.v1.json) | Proposal brief ingest — maps to `templates/BRIEF.md` |

Proposal output is markdown per [`templates/OUTPUT.md`](../templates/OUTPUT.md) (no JSON schema — fidelity validated against source offer artifact).

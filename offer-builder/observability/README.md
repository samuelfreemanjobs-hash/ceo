# Observability

Append one JSON line per phase (or per session) matching `trace-schema.json`.

**Failure tags to watch:** `skipped_discovery`, `generic_positioning`, `feature_list_value_prop`, `missing_proof_ladder`, `pricing_without_anchor`, `ignored_competitive_context`
